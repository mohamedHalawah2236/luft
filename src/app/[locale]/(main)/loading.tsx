import Loader from '@/components/shared/Loader';

function Loading() {
  return (
    <div className='flex h-screen min-h-dvh items-center justify-center'>
      <Loader />
    </div>
  );
}
export default Loading;
